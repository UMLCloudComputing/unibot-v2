# The Vector DB Engine: Milvus 🪵
Milvus is the vector database engine serving RAG requests.
It is deployed in HA mode as a cluster with it's own internal etcd database, proxy, and redundant io interfaces to the storage. 

## Details
Milvus is orchestrated through it's own operator and provisioned used CRDs defined by it. 
Milvus 

