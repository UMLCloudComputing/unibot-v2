githubConfigSecret: pre-defined-secret
template:
  spec:
    containers:
      - name: runner
        image: ghcr.io/actions/actions-runner:latest
        command: ["/home/runner/run.sh"]
        volumeMounts:
          - name: workdata
            mountPath: /home/runner/data  
    volumes:
      - name: workdata
        persistentVolumeClaim:
          claimName: runner-shared-data 
