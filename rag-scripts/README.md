# Testing locally
1. `docker-compose up -d`
2. Produce list of URLs to add and/or delete: `uv run rag-sync-manager.py`
3. To delete data: `uv run delete_data.py`
4. To add data: `uv run add_data.py`


