apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: unibot-operators
    namespace: argocd
    annotations:
        argocd.argoproj.io/sync-wave: "0"
spec:
    project: default
    source:
        repoURL: https://github.com/UMLCloudComputing/unibot-v2.git
        targetRevision: HEAD
        path: argocd/apps/operators
    destination:
        name: admin@unibot-cluster
        namespace: argocd
    syncPolicy:
        syncOptions:
            - CreateNamespace=true
        automated: 
            prune: true
            selfHeal: true
            allowEmpty: true 
